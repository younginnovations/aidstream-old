<li class="element-activity">

    <div class="element-title element-current">
        Activity Website
    </div>

    <div class="edit"><a href="<?php echo $this->baseUrl() ?>/activity/edit-element/?activity_id=<?php echo $this->activity_id; ?>&className=Activity_ActivityWebsite">Edit</a></div>
    <?php foreach ($this->elements as $element): ?>
        <div class="current">
            <ul class="style-2 arrow">
                <li> <span class="down-arrow"><?php if ($url = $element['text']): ?>
                            <a href="<?php echo $url; ?>" target="_blank">
                                <?php if (strlen($url) > 40): ?>
                                    <?php $out = str_split($url , 40); ?>
                                    <?php echo implode('<br>' , $out); ?>
                                <?php else: ?>
                                    <?php print ($url); ?>
                                <?php endif; ?>
                            </a>
                        <?php endif; ?>
                    </span>
                    <ul class="style-2-drop first">
                        <li>
                            <span class="name">Text:</span>
                            <span class="infom"><?php if ($url = $element['text']): ?>
                                    <a href="<?php echo $url; ?>" target="_blank">
                                        <?php if (strlen($url) > 40): ?>
                                            <?php $out = str_split($url , 40); ?>
                                            <?php echo implode('<br>' , $out); ?>
                                        <?php else: ?>
                                            <?php print ($url); ?>
                                        <?php endif; ?>
                                    </a>
                                <?php endif; ?></span>
                        </li>
                    </ul>
                </li>
            </ul>
        </div>
    <?php endforeach; ?>
</li><!-- end of element-activity -->
